formatter.name: pretty

suites:
    main:
        spec_prefix: Spec
        namespace: :vendor
        psr4_prefix: :vendor
        src_path: '%paths.config%/src'
        spec_path: '%paths.config%/tests'
